name: Job1 Pipeline
on:
  merge_group:
  pull_request:
    branches:
      - feature/test2
    types: [labeled]

jobs:
  Label-Checker:
    runs-on: ubuntu-latest
    steps:    
      - name: "Step1"
        run: |
          echo "Step 1"
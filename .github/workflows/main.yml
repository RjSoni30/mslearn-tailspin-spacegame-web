name: Testing Workflow
on:
  push:
  
jobs:
  Job1:
    runs-on: ubuntu-latest
    steps:
      - name: "This is step1"
        run: |
          echo "FORCE_RUN=" >> $GITHUB_ENV
          
      - name: "This is step2"
        if: ! ${{ env.FORCE_RUN }} == ""
        run: |
          echo "Inside step2"

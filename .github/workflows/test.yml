name: Test Workflow
on:
  push:
env:
  run_job: 'true'
jobs:
  Job1:
    if: ${{ env.run_job == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - name: "Step1"
        run: |
          echo "Step1"

  Job2:
    if: ${{ env.run_job == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: "Step1"
        run: |
          echo "Step1"

  Job3:
    needs: [Job1, Job2]
    runs-on: ubuntu-latest
    steps:
      - name: "Step1"
        run: |
          echo "Step1"
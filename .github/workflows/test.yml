name: Test Workflow
on:
  push:

jobs:
  Job1:
    runs-on: ubuntu-latest
    env:
      value1: 'this is value1'
    steps:
      - name: "Step1"
        run: |
          echo "Step1"
          echo "${value1}"

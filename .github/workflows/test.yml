name: Test Workflow
on:
  push:
jobs:
  Job1:
    runs-on: ubuntu-latest
    env:
      mule_key_name: "mule_key_value2" # env variable
      mule_key_value1: "Password@123" # input
      mule_key_value2: "Password@456" # input

    steps:
      - name: "Read Env Variable"
        run: |
          mule_key_name=${{ env.mule_key_name }}
          echo "2. Mule Key is: ${!mule_key_name}"